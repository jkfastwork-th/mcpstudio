# UI Google Theme Pass

Goal: improve readability while keeping the operator-focused information hierarchy.

The theme uses Google-inspired semantic colors:
- Blue: primary actions and active navigation
- Green: healthy/running
- Yellow: attention/degraded
- Red: failure/destructive

The main surface is light neutral to improve scanability. Text-size controls alter all explicit UI font sizes using a root scaling variable and persist per browser.
