# UI Focus Pass

This patch deliberately narrows the normal operator experience to three views:

- Home — health, active project sessions, ingress, alerts.
- Sessions — create/manage isolated project sessions.
- System — ingress/reliability, with all low-level diagnostics behind one Advanced disclosure.

No backend, auth, ingress, isolation, lease, or session-manager behavior is changed.
